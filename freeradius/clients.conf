# ==============================================================================
# FreeRADIUS Client Configuration - clients.conf
# Enterprise RADIUS NAS Gateway Definitions (Mikrotik / Cisco / Ruckus / Aruba)
# ==============================================================================

# Wildcard Client for Localhost & Docker Bridge Testing (secret: testing123)
client wildcard_testing {
    ipaddr      = 0.0.0.0/0
    secret      = testing123
    shortname   = wildcard_test
    nas_type    = other
}

# Wildcard Client for Production NAS Network Access (secret: RadiusSecretKey2026!)
client wildcard_production {
    ipaddr      = 0.0.0.0/0
    secret      = RadiusSecretKey2026!
    shortname   = wildcard_prod
    nas_type    = other
}


